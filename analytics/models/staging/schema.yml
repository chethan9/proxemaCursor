version: 2

models:
  - name: stg_orders
    description: Staging layer for orders — passes through application columns with light typing.
    columns:
      - name: order_id
        tests:
          - not_null
      - name: store_id
        tests:
          - not_null
