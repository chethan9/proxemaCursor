# Metabase SQL snippets (Proxema)

Use **Native query** in Metabase. Add a **SQL variable** named `store_id` (type: Text) and map it to your dashboard **`store_id`** filter (required for embed).

Folder [`sql/`](./sql/) contains optional queries that are awkward in the GUI (pivots, AOV, heatmap data).

Build walkthrough for all 17 Woo-style cards: [`../docs/METABASE_WOO_REPORTS_PLAYBOOK.md`](../docs/METABASE_WOO_REPORTS_PLAYBOOK.md).
