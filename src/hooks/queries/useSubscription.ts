<![CDATA[
import { useQuery } from "@tanstack/react-query";
import { getSubscriptionByClient } from "@/
...
 daysUntilLock(sub),
    refetch: q.refetch,
  };
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 883 chars.]