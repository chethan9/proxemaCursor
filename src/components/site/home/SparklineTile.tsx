<![CDATA[
import { Card, CardContent } from "@/components/ui/card";
import { Skeleton } from "@/compon
...
mount={Math.abs(delta).toFixed(1)} />
            <span>{subtext}</span>
          </div>
        )}
      </CardContent>
    </Card>
  );
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 3178 chars.]