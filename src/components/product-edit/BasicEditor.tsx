<![CDATA[
import { useState, useEffect, useMemo } from "react";
import { Card, CardContent } from "@/components/ui/c
...
ges, ...newOnes] };
            });
            setImageOpen(null);
          }}
        />
      )}
    </div>
  );
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 11797 chars.]