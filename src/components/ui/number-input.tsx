<![CDATA[
import * as React from "react";
import { Input } from "@/components/ui/input";
import { cn } f
...
xport function isValidNumericString(s: string, integer = false): boolean {
  return numericRegex(integer).test(s);
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 1684 chars.]