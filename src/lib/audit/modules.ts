/** Client-safe audit module identifiers used for activity filtering. */
export const AUDIT_MODULES = {
  sites: "sites",
  templates: "templates",
  billing: "billing",
  settings: "settings",
  admin: "admin",
  auth: "auth",
} as const;

