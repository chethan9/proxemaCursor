<![CDATA[
import { supabase } from "@/integrations/supabase/client";
import { supabaseAdmin } from "@/integrations
...
 && (await getCurrentUsage(clientId)).products >= quota.max_products_per_site;
  return !blocked;
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 2925 chars.]

[Tool result trimmed: kept first 100 chars and last 100 chars of 294 chars.]