<![CDATA[
import type { NextApiRequest } from "next";
import { supabaseAdmin } from "@/integration
...
  
...
n keyStr + ": {" + lines.join(", ") + "}";
    }
    return `${key}: ${JSON.stringify(v)}`;
  }).join(" · ");
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 4017 chars.]