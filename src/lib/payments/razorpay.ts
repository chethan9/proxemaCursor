import crypto from "crypto";
import type {
  PaymentGateway, ChargeRequest, ChargeInitiated, Pa
...
Boolean(this.envKeyId && this.envKeySecret);
  }
}

export const razorpayGateway: PaymentGateway = new RazorpayGateway();


[Tool result trimmed: kept first 100 chars and last 100 chars of 5275 chars.]