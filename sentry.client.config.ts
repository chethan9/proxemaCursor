<![CDATA[
import * as Sentry from "@sentry/nextjs";

const dsn = process.env.NEXT_PUBLIC_SENTRY_DSN;
const enabled
...
        delete b.data.password;
        delete b.data.token;
        delete b.data.api_key;
      }
      return b;
    });
  }
  return event;
}
]]>

[Tool result trimmed: kept first 100 chars and last 100 chars of 2071 chars.]